import { cspellConfig } from '@kitschpatrol/cspell-config'

export default cspellConfig({
	ignorePaths: ['itson.config.ts'],
	words: ['daemonization', 'itson', 'keytar', 'rcfile', 'bootout', 'svcinstall'],
})
