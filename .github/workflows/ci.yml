name: CI

on:
  push:
    branches: [main]
  pull_request: {}
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.event_name }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

jobs:
  ci:
    runs-on: ${{ matrix.os }}
    timeout-minutes: 20

    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm ci

      - name: Setup macOS Keychain for tests
        run: |
          set -euo pipefail

          KEYCHAIN_NAME="ci-test.keychain-db"
          KEYCHAIN_PASSWORD="ci-test-password"

          # Remove any leftover keychain from a previous run
          security delete-keychain "$KEYCHAIN_NAME" 2>/dev/null || true

          # Create and configure the temporary keychain
          security create-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_NAME"
          security set-keychain-settings -lut 21600 "$KEYCHAIN_NAME"
          security unlock-keychain -p "$KEYCHAIN_PASSWORD" "$KEYCHAIN_NAME"

          # Add the temporary keychain to the search list (prepend it)
          EXISTING_KEYCHAINS=$(security list-keychains -d user | tr -d '"' | tr '\n' ' ')
          security list-keychains -d user -s "$KEYCHAIN_NAME" $EXISTING_KEYCHAINS
          security default-keychain -s "$KEYCHAIN_NAME"

          # Verify the keychain is accessible
          echo "Keychain search list:"
          security list-keychains -d user
          echo "Default keychain:"
          security default-keychain

      - name: Ensure LaunchAgents directory exists
        run: mkdir -p ~/Library/LaunchAgents

      - name: Build
        run: pnpm run build

      - name: Generate CLI help for readme
        run: npx ksc-mdat fix || true

      - name: Lint
        run: pnpm run lint

      - name: Test
        run: pnpm run test

      - name: Cleanup macOS Keychain
        if: always()
        run: |
          KEYCHAIN_NAME="ci-test.keychain-db"
          security delete-keychain "$KEYCHAIN_NAME" 2>/dev/null || true
