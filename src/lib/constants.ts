export const KEYCHAIN_SERVICE = 'itson-cli'

/**
 * Name of the internal task that runs itson itself at startup. Reserved, since
 * a user task with the same name would share its launchd label.
 */
export const ITSON_TASK_NAME = 'Itson'
